declare const useRemoteAudio: (props: {
    peerId: string;
    onClose?: ((reason?: {
        code: number;
        tag: string;
        message: string;
    }) => void) | undefined;
    onPlayable?: ((data: {
        track: MediaStreamTrack;
        stream: MediaStream;
        label: 'audio';
    }) => void) | undefined;
}) => {
    state: "playable" | "unavailable" | "stopped" | "paused" | "available";
    track: MediaStreamTrack | null;
    stream: MediaStream | null;
    isAudioOn: boolean;
};

export { useRemoteAudio };
