import {
  useRemoteMedia
} from "./chunk-LW4AZNF4.js";

// src/hooks/useRemoteAudio.ts
var useRemoteAudio = (props) => {
  const { state, track, stream } = useRemoteMedia({
    label: "audio",
    peerId: props.peerId,
    onStopped: props.onClose,
    onPlayable: (data) => {
      props.onPlayable?.({
        label: "audio",
        track: data.track,
        stream: data.stream
      });
    }
  });
  const isAudioOn = stream ? true : false;
  return {
    state,
    track,
    stream,
    isAudioOn
  };
};

export {
  useRemoteAudio
};
//# sourceMappingURL=chunk-2L7BLA5J.js.map