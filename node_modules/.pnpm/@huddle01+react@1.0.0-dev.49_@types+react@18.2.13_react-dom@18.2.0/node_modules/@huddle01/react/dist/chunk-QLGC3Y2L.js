import {
  useRemoteMedia
} from "./chunk-LW4AZNF4.js";

// src/hooks/useRemoteScreenShare.ts
var useRemoteScreenShare = (props) => {
  const {
    state,
    stream: videoStream,
    track: videoTrack
  } = useRemoteMedia({
    label: "screen-share-video",
    peerId: props.peerId,
    onClose: props.onClose,
    onPlayable: (data) => {
      props.onPlayable?.({
        label: "screen-share-video",
        track: data.track,
        stream: data.stream
      });
    }
  });
  const { stream: audioStream, track: audioTrack } = useRemoteMedia({
    label: "screen-share-audio",
    peerId: props.peerId,
    onClose: props.onClose,
    onPlayable: (data) => {
      props.onPlayable?.({
        label: "screen-share-audio",
        track: data.track,
        stream: data.stream
      });
    }
  });
  return {
    videoStream,
    audioStream,
    videoTrack,
    audioTrack,
    state
  };
};

export {
  useRemoteScreenShare
};
//# sourceMappingURL=chunk-QLGC3Y2L.js.map